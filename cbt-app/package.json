{
  "name": "cbt-app",
  "version": "0.1.0",
  "private": true,
  "description": "Private self-hosted CBT tracking web app (single-user MVP)",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter @cbt/api dev",
    "dev:web": "pnpm --filter @cbt/web dev",
    "build": "pnpm --filter @cbt/shared build && pnpm --filter @cbt/api build && pnpm --filter @cbt/web build",
    "start": "pnpm --filter @cbt/api start",
    "seed": "pnpm --filter @cbt/api seed",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
